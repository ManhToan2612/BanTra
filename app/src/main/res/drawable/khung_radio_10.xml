<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Màu nền trắng -->
    <stroke
        android:width="2dp"
        android:color="#FFA500" />
    <corners android:radius="5dp" /> <!-- Để các góc là vuông -->
</shape>